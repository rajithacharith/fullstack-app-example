// config.js
// This file will be mounted as a config map in deployment environments.
// Update the API_BASE_URL as needed for each environment.

const config = {
  API_BASE_URL: 'http://localhost:5000',
};

export default config;
